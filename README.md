# PIPE-PoP-NODE-V0.2.6-UPDATE

# UPDATE YOUR NODE WITH THIS COMMAND

    rm -rf pop && pkill pop && wget https://dl.pipecdn.app/v0.2.6/pop -O pop && chmod +x pop && ./pop --refresh && ./pop --version && ./pop --status

  # TO DEPLOY PIPE PoP NODE FOR THE FIRST TIME, FOLLOW THE GUIDE BELOW
      
   https://github.com/TRUTH-NAZO/PIPE-NODE-DEPLYMENT
